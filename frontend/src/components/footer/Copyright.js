import { Text } from "@chakra-ui/layout";
import * as React from "react";

export const Copyright = (props) => (
  <Text fontSize="sm" {...props}>
    All images belong to Blizzard and Diablo dev team
  </Text>
);
